---
interface Props {
  title: string;
  description?: string;
  image?: string;
}

const { 
  title, 
  description = "PixelVerdict - High-precision gaming evaluations, technical benchmarks, and interactive hardware matrices.",
  image = "/og-default.png"
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site || 'https://pixelverdict.com');

const pixelLetters = "PIXEL".split('');
const verdictLetters = "VERDICT".split('');
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <link rel="canonical" href={canonicalURL} />
    <title>{title} | PixelVerdict</title>
    <meta name="title" content={`${title} | PixelVerdict`} />
    <meta name="description" content={description} />

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:ital,wght@0,500;0,700;0,800;1,400&family=Orbitron:wght@700;800;900&family=Press+Start+2P&display=swap" rel="stylesheet">

    <style is:global>
      @import "tailwindcss";

      :root {
        --font-heading: 'Orbitron', sans-serif;
        --font-mono: 'JetBrains Mono', monospace;
        --font-pixel: 'Press Start 2P', monospace;
      }

      html, body {
        background-color: #05070e !important;
        color: #f1f5f9;
        min-height: 100vh;
        overflow-x: hidden;
        margin: 0;
      }

      .font-heading { font-family: var(--font-heading) !important; }
      .font-mono-tech { font-family: var(--font-mono) !important; }
      .font-pixel-accent { font-family: var(--font-pixel) !important; }

      /* Visual Card Glassmorphism */
      .bg-\[\#131b2e\]\/80, .bg-\[\#131b2e\]\/70, .bg-\[\#131b2e\] {
        background-color: rgba(19, 27, 46, 0.75) !important;
        backdrop-filter: blur(12px);
      }
      
      .border-purple-900\/40, .border-purple-500\/30, .border-slate-800 {
        border-color: rgba(168, 85, 247, 0.35) !important;
      }

      /* Animated Ambient Background Elements */
      @keyframes floatPurpleOrb {
        0% { transform: translate(0px, 0px) scale(1); opacity: 0.6; }
        50% { transform: translate(180px, 100px) scale(1.3); opacity: 0.85; }
        100% { transform: translate(0px, 0px) scale(1); opacity: 0.6; }
      }

      @keyframes floatCyanOrb {
        0% { transform: translate(0px, 0px) scale(1); opacity: 0.5; }
        50% { transform: translate(-160px, -90px) scale(1.25); opacity: 0.8; }
        100% { transform: translate(0px, 0px) scale(1); opacity: 0.5; }
      }

      @keyframes gridPulse {
        0%, 100% { opacity: 0.12; }
        50% { opacity: 0.28; }
      }

      .bg-animated-purple {
        animation: floatPurpleOrb 12s ease-in-out infinite;
      }

      .bg-animated-cyan {
        animation: floatCyanOrb 15s ease-in-out infinite;
      }

      .bg-cyber-grid {
        background-image: 
          linear-gradient(to right, rgba(168, 85, 247, 0.15) 1px, transparent 1px),
          linear-gradient(to bottom, rgba(6, 182, 212, 0.15) 1px, transparent 1px);
        background-size: 60px 60px;
        animation: gridPulse 8s ease-in-out infinite;
      }

      /* Text Glow */
      .text-glow-cyan {
        text-shadow: 0 0 12px rgba(6, 182, 212, 0.6);
      }

      /* Logo Letter Hop Animation */
      @keyframes pixelHop {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-6px); }
      }

      .logo-letter {
        display: inline-block;
        transition: transform 0.2s ease;
      }

      .logo-container:hover .logo-letter {
        animation: pixelHop 0.35s ease-in-out;
        animation-delay: var(--delay);
      }

      /* Custom Scrollbar */
      ::-webkit-scrollbar { width: 8px; height: 8px; }
      ::-webkit-scrollbar-track { background: #05070e; }
      ::-webkit-scrollbar-thumb {
        background: #1e293b;
        border-radius: 4px;
        border: 1px solid rgba(168, 85, 247, 0.3);
      }
      ::-webkit-scrollbar-thumb:hover { background: #a855f7; }
    </style>
  </head>
  <body class="min-h-screen flex flex-col justify-between selection:bg-purple-600 selection:text-white relative">
    
    <!-- HIGH-VISIBILITY ANIMATED BACKGROUND LAYER -->
    <div class="fixed inset-0 pointer-events-none" style="z-index: 0;">
      <!-- Cyber Grid Overlay -->
      <div class="absolute inset-0 bg-cyber-grid"></div>

      <!-- Neon Purple Glowing Orb -->
      <div 
        className="bg-animated-purple absolute top-[-100px] left-[-100px] w-[650px] h-[650px] rounded-full"
        style="background: radial-gradient(circle, rgba(168, 85, 247, 0.4) 0%, rgba(168, 85, 247, 0) 70%); filter: blur(60px);"
      ></div>

      <!-- Neon Cyan Glowing Orb -->
      <div 
        className="bg-animated-cyan absolute top-[40%] right-[-100px] w-[600px] h-[600px] rounded-full"
        style="background: radial-gradient(circle, rgba(6, 182, 212, 0.35) 0%, rgba(6, 182, 212, 0) 70%); filter: blur(60px);"
      ></div>
    </div>

    <!-- Header Navigation -->
    <header class="sticky top-0 z-50 bg-[#0d1322]/90 border-b border-purple-500/30 backdrop-blur-md shadow-[0_4px_25px_rgba(0,0,0,0.6)]">
      <div class="max-w-6xl mx-auto px-4 py-3.5 flex items-center justify-between">
        <!-- Logo -->
        <a href="/" class="logo-container group flex items-center gap-3">
          <div class="w-9 h-9 bg-gradient-to-tr from-purple-600 via-purple-500 to-cyan-400 rounded-xl flex items-center justify-center font-bold text-white shadow-[0_0_18px_rgba(168,85,247,0.5)] border border-purple-300/40 group-hover:scale-110 transition-transform">
            <span class="font-pixel-accent text-[9px]">P</span>
          </div>

          <span class="font-heading font-black text-xl tracking-tight text-white flex items-center select-none">
            {pixelLetters.map((char, i) => (
              <span 
                key={`p-${i}`} 
                class="logo-letter group-hover:text-purple-300" 
                style={`--delay: ${i * 0.04}s`}
              >
                {char}
              </span>
            ))}
            {verdictLetters.map((char, i) => (
              <span 
                key={`v-${i}`} 
                class="logo-letter text-cyan-400 text-glow-cyan" 
                style={`--delay: ${(pixelLetters.length + i) * 0.04}s`}
              >
                {char}
              </span>
            ))}
          </span>
        </a>

        <!-- Links -->
        <nav class="flex items-center gap-6 font-mono-tech text-xs font-bold">
          <a href="/#reviews" class="text-slate-300 hover:text-cyan-400 transition-colors tracking-wider uppercase">// ALL REVIEWS</a>
          <a href="/#reviews" class="px-4 py-1.5 rounded-xl bg-purple-600 hover:bg-purple-500 text-white transition-all shadow-[0_0_15px_rgba(168,85,247,0.4)] border border-purple-400/40">
            SEARCH
          </a>
        </nav>
      </div>
    </header>

    <!-- Main Body Container -->
    <main class="max-w-6xl mx-auto px-4 py-8 w-full flex-grow relative" style="z-index: 10;">
      <slot />
    </main>

    <!-- Footer -->
    <footer class="border-t border-purple-900/30 bg-[#05070e] py-8 text-center text-xs font-mono-tech text-slate-500 relative" style="z-index: 10;">
      <div class="max-w-6xl mx-auto px-4 flex flex-col md:flex-row items-center justify-between gap-4">
        <div class="flex items-center gap-2">
          <span class="w-2.5 h-2.5 rounded-full bg-purple-500 shadow-[0_0_8px_#a855f7] animate-pulse"></span>
          <span class="font-heading font-bold text-slate-300 tracking-wider">PIXELVERDICT</span>
        </div>
        <p>© {new Date().getFullYear()} PixelVerdict. All rights reserved.</p>
        <div class="flex items-center gap-4 text-slate-400">
          <a href="#" class="hover:text-cyan-400 transition-colors">Privacy</a>
          <a href="#" class="hover:text-cyan-400 transition-colors">Terms</a>
        </div>
      </div>
    </footer>
  </body>
</html>